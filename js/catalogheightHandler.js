// const listContainer = document.querySelector('.catalog__cards-container');
// const list = document.querySelector('.catalog__cards');
// console.log('started');
// listContainer.style.minHeight = `${list.offsetHeight + 62}px`;
// // const resizeObserver = new ResizeObserver((entries) => {
// //   for (let entry of entries) {
// //     if (entry.target === list) {
// //       listContainer.style.minHeight = `${list.offsetHeight + 60}px`;
// //     }
// //   }
// // });

// // resizeObserver.observe(list);
